# Archived CareCheck multipage website

These files preserve the former CareCheck website that was replaced by CareCheck Ready Labels on August 3, 2026.

The exact deployable version is also preserved in the Git branch `codex/archive-carecheck-multipage-site-2026-08-03` at commit `6399ebc`.

This `_archive` directory and the `clean-temp` working directory are excluded from GitHub Pages by the repository's `_config.yml`.
